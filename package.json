{
  "name": "menu-management",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node src/app.js",
    "dev": "nodemon src/app.js",
    "gen:secret": "node -e \"const fs=require('fs');const path='.env';const secret=require('crypto').randomBytes(64).toString('hex');let env='';if(fs.existsSync(path)){env=fs.readFileSync(path,'utf8');if(env.includes('JWT_SECRET=')){env=env.replace(/JWT_SECRET=.*/,'JWT_SECRET='+secret);}else{env+='\\nJWT_SECRET='+secret;}}else{env='JWT_SECRET='+secret;}fs.writeFileSync(path,env);console.log('JWT_SECRET updated in .env');\""
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Firlans/menu-management.git"
  },
  "author": "firlans",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Firlans/menu-management/issues"
  },
  "homepage": "https://github.com/Firlans/menu-management#readme",
  "description": "",
  "dependencies": {
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.16.3"
  },
  "type": "module",
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
